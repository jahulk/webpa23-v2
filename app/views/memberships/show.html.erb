

<%= render @membership %>

<div>
  <%= link_to("Edit this membership", edit_membership_path(@membership)) if current_user %>
  <%= " | " if current_user %>
  <%= link_to "Back to memberships", memberships_path %>

  <%= button_to("Destroy this membership", @membership, method: :delete) if current_user %>
</div>
