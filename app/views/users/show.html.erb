<p style="color: green"><%= notice %></p>

<div>
  <h2>Username:</h2>
  <p><%= @user.username %></p>
  <% if @user.ratings.empty? %>
    <p>Has not made any ratings yet!</p>
  <% else %>
    <p>Has made <%= pluralize(@user.ratings.count, 'ratings') %>, average rating <%= @user.average_rating %></p>
  <% end %>
</div>
<div>
  <p>
    <strong>member of</strong>
  </p>
  <ul>
    <% @user.beer_clubs.each do |beer_club| %>
      <li id="<%= dom_id beer_club %>">
        <%= link_to beer_club.name, beer_club %>
      </li>
    <% end %>
  </ul>
  <p>
</div>
<div>
  <h3>ratings</h3>
  <ul>
    <% @user.ratings.each do |rating| %>
      <li>
        <%= rating.to_s %>
        <%= link_to "Delete", rating, data: {
          "turbo-method": :delete,
          "turbo_confirm": "Are you sure?"
        } %>
      </li>
    <% end %>
  </ul>
</div>
<div>
  <%= link_to "Edit this user", edit_user_path(@user) %> |
  <%= link_to "Back to users", users_path %>

  <%= button_to "Destroy this user", @user, method: :delete %>
</div>
